import matplotlib.pyplot as plt
import numpy as np

# 2x2 grid: rows = protocols, columns = metrics
fig, axes = plt.subplots(2, 2, figsize=(13, 11))

# === Top row: Within-session ===

# Within-session force
ax = axes[0, 0]
ax.scatter(targets_ws[:, 0], preds_ws[:, 0], alpha=0.4, color="#2E5BBA", s=40)
ax.plot([50, 300], [50, 300], "r--", linewidth=2)
ax.set_xlabel("True peak force (N)", fontsize=13)
ax.set_ylabel("Predicted peak force (N)", fontsize=13)
ax.set_title("Within-Session: Force\nMAE 16.0N, R² 0.81", fontsize=14)
ax.set_xlim(40, 320)
ax.set_ylim(40, 320)
ax.grid(alpha=0.3)
ax.set_aspect("equal")

# Within-session speed
ax = axes[0, 1]
ax.scatter(targets_ws[:, 1], preds_ws[:, 1], alpha=0.4, color="#2E5BBA", s=40)
ax.plot([2, 8], [2, 8], "r--", linewidth=2)
ax.set_xlabel("True peak speed (m/s)", fontsize=13)
ax.set_ylabel("Predicted peak speed (m/s)", fontsize=13)
ax.set_title("Within-Session: Speed\nMAE 0.30m/s, R² 0.86", fontsize=14)
ax.set_xlim(2, 8)
ax.set_ylim(2, 8)
ax.grid(alpha=0.3)
ax.set_aspect("equal")

# === Bottom row: Cross-subject ===

# Cross-subject force
ax = axes[1, 0]
ax.scatter(targets_cv[:, 0], preds_cv[:, 0], alpha=0.4, color="#2E5BBA", s=40)
ax.plot([50, 300], [50, 300], "r--", linewidth=2)
ax.set_xlabel("True peak force (N)", fontsize=13)
ax.set_ylabel("Predicted peak force (N)", fontsize=13)
ax.set_title("Cross-Subject: Force\nMAE 43.4N, R² -0.27", fontsize=14)
ax.set_xlim(40, 320)
ax.set_ylim(40, 320)
ax.grid(alpha=0.3)
ax.set_aspect("equal")

# Cross-subject speed
ax = axes[1, 1]
ax.scatter(targets_cv[:, 1], preds_cv[:, 1], alpha=0.4, color="#2E5BBA", s=40)
ax.plot([2, 8], [2, 8], "r--", linewidth=2)
ax.set_xlabel("True peak speed (m/s)", fontsize=13)
ax.set_ylabel("Predicted peak speed (m/s)", fontsize=13)
ax.set_title("Cross-Subject: Speed\nMAE 0.88m/s, R² -0.05", fontsize=14)
ax.set_xlim(2, 8)
ax.set_ylim(2, 8)
ax.grid(alpha=0.3)
ax.set_aspect("equal")

plt.tight_layout()
plt.savefig("slide3_all_results.png", dpi=150, bbox_inches="tight")
plt.show()
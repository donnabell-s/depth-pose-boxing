# ── PyTorch (CUDA 13.0 — default as of PyTorch 2.11) ─────────────────
torch
torchvision
torchaudio

# ── Core ──────────────────────────────────────────────────────────────
numpy>=1.24,<2.0        # pinned — keep below 2.0 for broader compatibility
opencv-python>=4.8
scipy>=1.11
pandas>=2.0             # IMU CSV processing

# ── Pose estimation ───────────────────────────────────────────────────
ultralytics>=8.0        # YOLOv8-Pose — downloads model weights on first run

# ── Depth Anything V2 (relative models via HuggingFace) ───────────────
transformers>=4.40
Pillow>=10.0

# ── IMU hardware integration (Phase 2B) ──────────────────────────────
pyserial>=3.5           # USB serial communication with ESP/Feather
keyboard>=0.13          # Q-key stop for receive scripts

# ── Visualisation ─────────────────────────────────────────────────────
matplotlib>=3.8
jupyter                 # for notebooks/visualize.ipynb